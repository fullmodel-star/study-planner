# 更新記錄

> 一支一檔規則：本 App 有任何異動就在最上面新增一筆。總覽見 `Desktop\App專案\ARCHITECTURE.md`。

## 2026-08-06 · 桌面安裝 icon 同步稜線新版

icon 換成稜線新版 icon-only（行事曆線條圖）。原本是舊版「山形＋LEARNING SCHEDULE 文字＋簽名」。來源＝`05_品牌資源\09_對外網站_v4\dist\assets\icons\studyplan-512.png`，同步 icon-192/512＋apple-touch-icon＋favicon.svg＋icon.svg；`icon-maskable-512.png` 另外重做，圖案限縮到中央 80% safe zone（原本跟一般 icon 同一張，會被系統圓形遮罩切到）。sw cache `study-planner-v26-ninjaxp-reset`→`study-planner-v27-icon`。

⚠️ **本次踩到的坑**：push 後 GitHub Pages 的 legacy Jekyll build 連續回報 `Page build failed.`／`Deployment cancelled`，新 icon 上不去。原因是連續三次推送讓 workflow 互相取消。已加 `.nojekyll`（本專案是純靜態 PWA，不需要 Jekyll 處理），並用 `gh api -X POST repos/fullmodel-star/study-planner/pages/builds` 重新觸發後建置成功。**下次遇到 Pages 卡住，先看 `gh run list -R <repo>` 而不是只看 pages API 的 status，cancelled 多半是自己連推造成的，不是內容有問題。**
